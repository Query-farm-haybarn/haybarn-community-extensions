docs:
  extended_description: For more information regarding usage, see the [documentation](https://query.farm/duckdb_extension_tributary.html).
extension:
  # Haybarn: disabled because the extension does not yet build
  # against DuckDB 1.5.2 — upstream community-extensions also has
  # no v1.5.2 binary. Re-enable when upstream lands a v1.5.2-
  # compatible release (sync_from_upstream will pick up the bump).
  # Query Farm-owned repo (query-farm/tributary) — fix internally
  excluded_platforms: linux_amd64;linux_arm64;linux_amd64_musl;linux_arm64_musl;osx_amd64;osx_arm64;windows_amd64;windows_arm64;windows_amd64_mingw;wasm_mvp;wasm_eh;wasm_threads
  build: cmake
  description: Enable DuckDB to interact with Apache Kafka
  language: C++
  license: MIT
  maintainers:
  - rustyconover
  name: tributary
  version: '2025120401'
repo:
  github: query-farm/tributary
  ref: 36faa17973a9b45180871e4db6929a382b3214ab
